var myName = 'Gordon';

function sayName() {
    var secret = 'watchandcode';
    console.log(myName);
}

sayName();
console.log(secret);

// If you are inside of a function, you can look out and see data, but the opposite is NOT true. If you are outside of a function, you cannot look into a function.
//